---
env_system_mount: /mnt

env_mount_common: async,noatime,auto,dev,exec,noiversion,suid,rw,nouser
env_mount_root: "{{ env_mount_common }},dax=always,nodiscard,noquota"
env_mount_efi: >-
  {{ env_mount_common }},fmask=0023,dmask=0022,check=s,discard,
  errors=remount-ro,iocharset=utf8,tz=UTC,flush,utf8

env_locale: en_US.UTF-8
env_keymap: us
env_timezone: Europe/Berlin
env_hostname: hostname
#
# Root password hash made by mkpasswd, value is `test`.
#
env_root_hash: $y$j9T$bc6ILThq29/KeZ4nsvExl.$G81Y9dCNivTbZ75JCyVNbduZzI3dxMwwW185EMeIqsC

env_user: xnd
env_user_name: User
env_user_full_name: User Name
env_user_alt_name: Nom d'Utilisateur
env_user_email: example@.local
#
# User password, with vault expansion, vault is `test`.
#
env_user_password: !vault |
  $ANSIBLE_VAULT;1.1;AES256
  
  6463
env_gpg_main_password: test
env_gpg_compat_password: testtest
env_gpg_priv_password: test
env_gpg_expire: 5y

env_monitors:
  - name: primary
    workspaces: 8
    width: 1920
    height: 1080
    refresh: 60
    x11: DFP-1
    wayland: HDMI-A-1

  - name: secondary
    workspaces: 4
    width: 1280
    height: 1024
    refresh: 75
    x11: DFP-5
    wayland: HDMI-A-3
    notifications: true

env_monitors_stack: horizontal

#
# Assuming that virtual perfect monitor has the following DPI.
# All spacings are based on this value.
#
env_ethalon: 96
#
# Real monitor is different.
#
# Dpi is a very tricky metric, because it is up to the application
# how to implement scaling. For example:
#   herbstluftwm
#     scales font,
#     does not scale paddings, offsets, etc.
#   polybar
#     scales spaces, points, both :size and :pixelsize,
#     does not scale pixels and font offsets.
#   dunst
#     scales sizes, spacings, and font when dpi is greater than 96,
#     but only (down)scales font when dpi is smaller than 96
#   and so on.
#
#   While it is okay for the regular user applications to be scaled arbitrary,
#   it is not the case for the WM & related programs.
#
#   For instance, when herbstluftwm draws a 5 pixels border around window,
#   it is quite noticable when rofi draws about 7 pixels. 
#   Another example is that changing dpi for polybar will not affect other
#   programs, as well as will break polybar itself, because font offset
#   is not affected by the DPI.
#
#   Practically it means that it is impossible to create a dpi-aware stack
#   of the window manager environment. Therefore, using its own dpi-like mechanism,
#   where elements are being scaled at the template rendering stage.
#
env_scale: 0.9

#
# Spacing in pixels, measured for virtual env_ethalon.
#
env_space_thin: 1
env_space_light: 2
env_space_normal: 6
env_space_big: 11

env_font_monospace: Source Code Pro
env_font_monospace_package: adobe-source-code-pro-fonts
env_font_serif: Inter
env_font_serif_package: inter-font
env_font_sans_serif: Inter
env_font_sans_serif_package: inter-font
env_font_emoji: JoyPixels
env_font_emoji_package: ttf-joypixels
env_font_icon: Symbols Nerd Font Mono
env_font_icon_package: ttf-nerd-fonts-symbols-mono

env_font_size: 13
env_font_offset: 2

env_font_size_big: 16
env_font_offset_big: 3

env_font_icon_size: 15
env_font_icon_offset: 2
