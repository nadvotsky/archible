Section "InputClass"
    Identifier "mouse"
    Driver "libinput"

    MatchIsPointer "on"

    Option "AccelProfile" "{{ inputs.mouse.accel }}"
    Option "AccelSpeed" "{{ inputs.mouse.speed }}"
EndSection
